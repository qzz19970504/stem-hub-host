# stem-hub 上位机 Python 依赖
# 安装: pip install -r requirements.txt
# 推荐使用清华源: pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt

# --- GUI 框架 ---
# PySide6 用 pip 装, conda 包不带 QtSerialPort 等 GPL 模块
PySide6>=6.8
PySide6-Addons>=6.8

# --- 实时绘图 ---
pyqtgraph>=0.13

# --- 数值 / 数据处理 ---
numpy>=1.24
